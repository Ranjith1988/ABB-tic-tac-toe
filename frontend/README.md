# Tic Tac Toe Frontend

Angular standalone application for the Tic Tac Toe exercise. The frontend intentionally contains presentation/orchestration logic only; the backend owns the game rules and state.

Run with `npm install` and `npm start`.
